{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch current file via Live Server",
            "url": "http://127.0.0.1:5500/${relativeFile}",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
